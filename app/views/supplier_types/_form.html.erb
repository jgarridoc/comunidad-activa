<%= form_with(model: supplier_type) do |form| %>
  <% if supplier_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supplier_type.errors.count, "error") %> prohibited this supplier_type from being saved:</h2>

      <ul>
        <% supplier_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tipo_proveedor %>
    <%= form.text_field :tipo_proveedor %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
